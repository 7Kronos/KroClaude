---
name: db-reviewer
description: Smoke-test fixture sub-agent for feature 005-config-bundling.
model: sonnet
---

# db-reviewer (smoke fixture)

This is a minimal agent.md used by `tests/smoke/test_us6.sh` to verify
the `reflect_dir_of_dirs` helper places agent directories correctly
under `~/.claude/agents/<name>/`.
