---
description: "Triage incoming issues by labeling and assigning."
---

# /triage (smoke fixture)

This is a minimal slash-command file used by `tests/smoke/test_us6.sh`
to verify the `reflect_dir_of_files` helper places `*.md` files into
`~/.claude/commands/` correctly.
